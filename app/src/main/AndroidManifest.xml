<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meridianid.farizdotid.mahasiswaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PT.BankSUMUT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- <activity android:name=".activity.LoginActivity"> -->
        <activity android:name=".activity.Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.DosenActivity" />
        <activity android:name=".activity.MatkulActivity" />
        <activity android:name=".activity.TambahMatkulActivity" />
        <activity android:name=".activity.MatkulDetailActivity" />
        <activity android:name=".activity.TambahMatkulActivity2" />
        <activity android:name=".activity.KantorActivity" />
        <activity android:name=".activity.ATMActivity" />
        <activity android:name=".activity.ChangeProfileActivity" />
    </application>

</manifest>